<%- include('../partials/header') %>

    <form class='AddIngForm' action="/ingredients" method="POST">
        <input class='AddIngForm' type="text" name="ingredient" value="Input Ingredients">
        <select class='AddIngForm' name="category">
            <option value="Fruit and Vegetables">Fruit and Vegetables</option>
            <option value="Starch">Starch</option>
            <option value="Dairy">Dairy</option>
            <option value="Protein">Protein</option>
            <option value="Fat">Fat</option>
        </select>
        Like?<input class='AddIngForm' type="checkbox" name="like" checked>
        <input type="submit">
    </form>

    <table>
        <tr>
            <th>Ingredient</th>
            <th>Category</th>
            <th>Like?</th>
        </tr>
        <tbody>
            <% ingredient.forEach(i=> { %>
                <tr>
                    <td>
                        <%= i.ingredient %>
                    </td>
                    <td>
                        <%= i.category %>
                    </td>
                    <td>
                        <%= i.like ? 'Yes' : 'Nope' %>
                    </td>
                    <td>
                        <% if (user && user._id.equals(i.user)) { %>
                        <form action="/ingredients?_method=DELETE" method="POST">
                            <input type="hidden" name='remove' value="<%=i._id%>">
                            <button type="submit">Remove</button>
                        </form>
                        <% } %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>

    <%- include('../partials/footer') %>