<%- include('../partials/header') %>

<table id="allRecipesTable">
    <tbody>
        <% recipes.forEach(r => { %>
            <tr>
            <td><a id='tableContent' href="/recipes/<%= r.id %>"><%= r.title%></a></td>
            </tr>
        <% }) %>
    </tbody>
</table>
  
<%- include('../partials/footer') %>